#include <bits/stdc++.h>
using namespace std;
void wave(vector <int> &arr){
    int n=arr.size();
    vector<int> temp(n);
    
    for(int i=0;i<n-1;i+=2){
        swap(arr[i], arr[i+1]);
        
    }
}
int main() {
	vector<int> arr={1,2,3,4,5};
	wave(arr);
    for(int num:arr){
        cout<<num;
    }

	
}
